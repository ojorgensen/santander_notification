# Email Configuration
RECIPIENT_EMAIL=your-notification-email@example.com

# Note: Authentication is now handled through OAuth2 via the Gmail API.
# You need to create a project in the Google Cloud Console and download credentials.json.
# The application will guide you through the authentication process on first run. 